<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="myproject.dao.EquipmentRentalDao">

    <resultMap type="myproject.entity.EquipmentRentalEntity" id="equipmentRentalMap">
        <result property="id" column="id"/>
        <result property="equipmentid" column="equipmentid"/>
        <result property="equipmentname" column="equipmentname"/>
        <result property="userid" column="userid"/>
        <result property="username" column="username"/>
        <result property="quantity" column="quantity"/>
        <result property="rentaltime" column="rentaltime"/>
        <result property="expectedreturntime" column="expectedreturntime"/>
        <result property="actualreturntime" column="actualreturntime"/>
        <result property="status" column="status"/>
        <result property="approvalstatus" column="approvalstatus"/>
        <result property="approvalremark" column="approvalremark"/>
        <result property="remark" column="remark"/>
        <result property="adduserid" column="adduserid"/>
        <result property="addusername" column="addusername"/>
        <result property="addtime" column="addtime"/>
    </resultMap>
        <select id="selectValue" resultType="java.util.Map">
        SELECT ${params.xColumn}, sum(${params.yColumn}) total FROM equipment_rental
        <where>${ew.sqlSegment}</where>
        group by ${params.xColumn}
        limit 10
    </select>
        <select id="selectTimeStatValue" resultType="java.util.Map">
        <if test='params.timeStatType == "日"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m-%d') ${params.xColumn}, sum(${params.yColumn}) equipment
            FROM
            equipment_rental
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m-%d')
        </if>
        <if test='params.timeStatType == "月"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn}, sum(${params.yColumn}) equipment  FROM
            equipment_rental
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        <if test='params.timeStatType == "年"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn}, sum(${params.yColumn}) equipment  FROM
            equipment_rental
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        order by ${params.xColumn}
    </select>
        <select id="selectGroup" resultType="java.util.Map">
        SELECT ${params.column} , count(1) equipment  FROM equipment_rental
        <where>${ew.sqlSegment}</where>
        group by ${params.column}
        limit 10
    </select>
</mapper>